/* istanbul ignore file */
import { CommitlintConfig } from '../types'

/* istanbul ignore next */
const commitlintConfig: CommitlintConfig = {
  extends: ['@commitlint/config-conventional'],
}

/* istanbul ignore next */
export default commitlintConfig
