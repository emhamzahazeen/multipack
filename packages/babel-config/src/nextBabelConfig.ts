/* istanbul ignore file */
import { BabelConfigPart } from '../types'

/* istanbul ignore next */
const nextBabelConfig: BabelConfigPart = {
  presets: ['next/babel'],
}

/* istanbul ignore next */
export default nextBabelConfig
